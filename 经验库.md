# 经验库

> ⚠️ 铁律：每次思考问题前先翻这里。说"不能"之前对照第 1 条。补丁打到第 2 轮对照第 2 条。

---

## 001 | ❌ 错误 | 工具意识脱节

**场景：** 微信用户问"能识别图片吗"，直接答"不能"
**根因：** 把"获取图片"和"收微信消息"绑定了，忘了手里有 read_file / run_command 能读本地文件
**教训：说"不能"之前先问 3 个问题：①核心功能是什么？②我有哪些工具？③有没有绕过限制的第三条路？**
**正确做法：** 引导用户把图片存到本地文件夹，用工具读取识别

---

## 002 | ❌ 错误 | 控制台 UI 盲猜 4 轮不回头

**场景：** API 正常但前端不显示聊天记录。连修缓存头、DOM 事件、正则、错误处理——全是猜的
**根因：** "API 对 = UI 对"的错误假设。没有第一时间写极简验证页隔离问题
**教训：排查看不见的 UI，第一轮就写 30 行极简验证页。补丁超过 2 轮不解决 → 退回极简版重新定位**

---

## 003 | ❌ 错误 | 修表象不查根因

**场景：** QR 码连续显示"过期"，原地加刷新逻辑，没查服务端实际返回了什么
**根因：** 急于在业务逻辑层打补丁，没有先写 debug 脚本查原始数据
**教训：异常出现先写最小 debug 脚本看裸返回，确认根因后再改代码**

---

## 004 | ❌ 错误 | 过度复杂化

**场景：** 控制台从 200 行膨胀到 360 行，加了 DOMContentLoaded→switchChatUser→viewChat 三层调用链、escapeHtml 双定义、fetch 封装、正则替换。最后推翻，测试页 30 行直接解决
**根因：** 功能能跑后还在叠抽象，没有停下来问"最简单版本是什么样"
**教训：代码在膨胀时停下来，自问：200 行能解决的事为什么写到 360 行？**

---

## 005 | ❌ 错误 | 进程管理粗暴

**场景：** 多次 Stop-Process 失败，旧 node 进程残留占端口 8899，新旧 dashboard 同时跑
**根因：** 杀进程后没验证端口是否释放就启动新服务
**教训：杀进程后 → netstat 验证端口已释放 → 再启动新服务。两步不能省**

---

## 006 | ❌ 错误 | 格式没确认就产出

**场景：** 用户说"我要文档模式"的简历，第一版给了 HTML 浏览器版
**根因：** 没确认用户要的格式就默认生成
**教训：产出物格式先确认，不能默认自己猜的对**
