# 经验教训记录

> ⚠️ 每次思考问题前先翻这里。添加记录永远追加，不覆盖删除。
> 每条必须有**通用结论**——≤20字跨场景原则。

---

## 1. ❌ PPT生成没一步到位
- **场景**：PPT脚本写完了没运行，等用户催才执行
- **教训**：写完直接跑，输出最终文件
- **通用结论**：产出物一步到位，不等催

## 2. ❌ 文件传输让用户绕路
- **场景**：用户要微信发文件，我答"做不到"，让用户自己拖文件/打开文件夹/GitHub链接
- **教训**：遇到限制先问"最快让用户拿到结果的方式是什么"，主动执行
- **通用结论**：别让用户替你动手

## 3. ❌ 工具意识脱节
- **场景**：用户问"能识别图片吗"，直接答"不能"
- **根因**：忘了手上有read_file/run_command能读本地文件
- **教训**：说"不能"之前先问：①核心功能？②有什么工具？③有无绕过限制的路？
- **通用结论**：说"不能"前先扫全部工具

## 4. ❌ 控制台UI盲猜4轮
- **场景**：API正常但前端不显示聊天记录，连修4轮全是猜的
- **根因**："API对=UI对"的错误假设
- **教训**：排查看不见的UI，第一轮先写30行极简验证页
- **通用结论**：先写最小验证页再修bug

## 5. ❌ 修表象不查根因
- **场景**：QR码连续过期，原地加刷新逻辑，没查服务器实际返回
- **根因**：急于打补丁，没先写debug脚本看裸数据
- **教训**：异常先写最小debug脚本，确认根因再改
- **通用结论**：先查根因再修，别猜

## 6. ❌ 过度复杂化
- **场景**：控制台200行膨胀到360行，三层调用链+各种封装。最后30行推翻
- **根因**：功能能跑后还叠抽象
- **教训**：代码膨胀时停下来问"最简单版本什么样"
- **通用结论**：代码膨胀先简化后继续

## 7. ❌ 进程管理粗暴
- **场景**：多次Stop-Process失败，旧进程残留占端口
- **根因**：杀完没验证就启新的
- **教训**：杀进程→netstat验证→再启动
- **通用结论**：操作后必须验证再下一步

## 8. ❌ 格式没确认就产出
- **场景**：用户要Word文档简历，给了HTML版
- **教训**：产出物格式先问清楚
- **通用结论**：先确认格式再动手

## 9. ❌ 验证环境≠部署环境
- **场景**：Windows.Media.Ocr交互式PS通过，execSync里全报TypeNotFound
- **根因**：交互式PS和子进程是两个环境
- **教训**：测试必须在目标环境下做
- **通用结论**：部署环境做测试才算数

## 10. ❌ 系统提示词没跟功能更新
- **场景**：OCR接入后Claude还在翻images文件夹叫用户手动放图
- **根因**：AI不会自动感知代码变化
- **教训**：功能上线同步更新系统提示词
- **通用结论**：改代码必须同步改prompt

## 11. ❌ PPT需求没对齐就批量产出
- **场景**：没问主题/色系就做5页，全部返工
- **教训**：第一版先出1页样稿确认方向再批量
- **通用结论**：先出样稿再批量，不猜

## ✅ 15. 批量图片工具先逐张审阅，再做4K增强
- **做法**：用 Node + sharp 检测和裁剪截图黑边，前端逐张接受/跳过/手动微调；再调用本机 Real-ESRGAN ncnn 逐张生成 `_4k.png`，保留原裁剪图。
- **模式**：高风险批量处理不一键全跑，把机器处理与人工审阅拆成两步；重型模型优先选可本地运行的 ncnn/vendor 包，并同时提供快速与最高画质选项。

---

## ✅ 1-15 成功经验速查
| # | 通用结论 |
|---|---------|
| 1 | 初始化开销大的用懒加载单例 |
| 2 | 后台全自动，用户只看结果 |
| 3 | 重复流程写入系统自动执行 |
| 4 | 视觉需求直接走最丰富路线 |
| 5 | 用极端颜色标记定位渲染bug |
| 6 | 交互方式匹配游戏类型直觉 |
| 7 | 用户说需求，系统自动匹配专家 |
| 8 | 查表比扫描全部更快更省 |
| 9 | MCP接入统一npx -y模式批量装 |
| 10 | 钩子采数据+LLM做决策分工协作 |
| 11 | 项目配置MCP/钩子/权限一把配 |
| 12 | Windows MCP必须.mcp.json+cmd/c |
| 13 | 报错别静默，追到根因才算完 |
| 14 | 先统一重复逻辑，再增量加功能 |
| 15 | 批量图片处理先逐张审阅，再做4K增强 |

---

## 核心铁律
1. **先想后动** — 说不能前扫工具，修bug前写验证，加代码前问简化
2. **一步到位** — 输出最终文件，不等催不等确认
3. **环境一致** — 验证和部署用同一个环境
