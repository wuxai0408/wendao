# 经验教训记录

> ⚠️ 每次思考问题前先翻这里。添加记录永远追加，不覆盖删除。
> 每条必须有**通用结论**——≤20字跨场景原则。

---

## 1. ❌ PPT生成没一步到位
- **场景**：PPT脚本写完了没运行，等用户催才执行
- **教训**：写完直接跑，输出最终文件
- **通用结论**：产出物一步到位，不等催

## 2. ❌ 文件传输让用户绕路
- **场景**：用户要微信发文件，我答"做不到"，让用户自己拖文件/打开文件夹/GitHub链接
- **教训**：遇到限制先问"最快让用户拿到结果的方式是什么"，主动执行
- **通用结论**：别让用户替你动手

## 3. ❌ 工具意识脱节
- **场景**：用户问"能识别图片吗"，直接答"不能"
- **根因**：忘了手上有read_file/run_command能读本地文件
- **教训**：说"不能"之前先问：①核心功能？②有什么工具？③有无绕过限制的路？
- **通用结论**：说"不能"前先扫全部工具

## 4. ❌ 控制台UI盲猜4轮
- **场景**：API正常但前端不显示聊天记录，连修4轮全是猜的
- **根因**："API对=UI对"的错误假设
- **教训**：排查看不见的UI，第一轮先写30行极简验证页
- **通用结论**：先写最小验证页再修bug

## 5. ❌ 修表象不查根因
- **场景**：QR码连续过期，原地加刷新逻辑，没查服务器实际返回
- **根因**：急于打补丁，没先写debug脚本看裸数据
- **教训**：异常先写最小debug脚本，确认根因再改
- **通用结论**：先查根因再修，别猜

## 6. ❌ 过度复杂化
- **场景**：控制台200行膨胀到360行，三层调用链+各种封装。最后30行推翻
- **根因**：功能能跑后还叠抽象
- **教训**：代码膨胀时停下来问"最简单版本什么样"
- **通用结论**：代码膨胀先简化后继续

## 7. ❌ 进程管理粗暴
- **场景**：多次Stop-Process失败，旧进程残留占端口
- **根因**：杀完没验证就启新的
- **教训**：杀进程→netstat验证→再启动
- **通用结论**：操作后必须验证再下一步

## 8. ❌ 格式没确认就产出
- **场景**：用户要Word文档简历，给了HTML版
- **教训**：产出物格式先问清楚
- **通用结论**：先确认格式再动手

## 9. ❌ 验证环境≠部署环境
- **场景**：Windows.Media.Ocr交互式PS通过，execSync里全报TypeNotFound
- **根因**：交互式PS和子进程是两个环境
- **教训**：测试必须在目标环境下做
- **通用结论**：部署环境做测试才算数

## 10. ❌ 系统提示词没跟功能更新
- **场景**：OCR接入后Claude还在翻images文件夹叫用户手动放图
- **根因**：AI不会自动感知代码变化
- **教训**：功能上线同步更新系统提示词
- **通用结论**：改代码必须同步改prompt

## 11. ❌ PPT需求没对齐就批量产出
- **场景**：没问主题/色系就做5页，全部返工
- **教训**：第一版先出1页样稿确认方向再批量
- **通用结论**：先出样稿再批量，不猜

---

## ✅ 1. Worker预加载单例
- **做法**：`_ocrWorker`全局缓存，createWorker一次永久复用
- **通用结论**：初始化开销大的用懒加载单例

## ✅ 2. 全自动OCR管线
- **做法**：微信收图→CDN下载→AES解密→OCR→传Claude，用户零感知
- **通用结论**：后台全自动，用户只看结果

## ✅ 3. 自动化经验记录系统
- **做法**：CLAUDE.md设定自动触发→读库→总结→追加→记忆→git推送
- **通用结论**：重复流程写入系统自动执行

## ✅ 4. 精美PPT视觉方案
- **做法**：Unsplash高清图背景+半透明遮罩+金色装饰+多色系+装饰角标
- **通用结论**：视觉需求直接走最丰富路线

## ✅ 5. 调试标记法
- **做法**：在不确定位置画亮黄/亮粉方块，10秒定位渲染管线问题
- **通用结论**：用极端颜色标记定位渲染bug

## ✅ 6. 方向键探索+碰撞战斗
- **做法**：WASD/方向键走地图，撞怪触发回合制，全清自动新地图
- **通用结论**：交互方式匹配游戏类型直觉

## ✅ 7. 自动Agent匹配系统
- **做法**：扫描171个agent→提取关键词构建索引→CLAUDE.md设定自动匹配规则→用户提需求时自动加载最相关专家
- **通用结论**：用户说需求，系统自动匹配专家，零手动

## ✅ 8. Agent映射表优化——全量索引改为关键词查表
- **做法**：171个agent的全量JSON索引（30KB/每次10K token）→ 56个分类的关键词映射表（8KB/每次500 token），CLAUDE.md指向轻量映射表
- **通用结论**：查表比扫描全部更快更省

---

## ✅ 9. MCP服务器批量接入
- **做法**：npm全局安装@modelcontextprotocol/*系列包 → settings.json配置mcpServers → npx -y自动拉取运行
- **可参考的点**：统一用npx -y模式无需预装，包名命名空间一致方便批量；用npm list -g验证安装状态
- **通用结论**：MCP接入统一npx -y模式批量装

## ✅ 10. 自动进化系统——钩子+LLM四阶循环
- **做法**：on-start.ps1创建会话审计 → on-stop.ps1统计+信号检测+写flags → LLM启动时读flags决策是否出改进提案 → 验证循环
- **可参考的点**：钩子做数据采集+简单阈值判断，LLM做语义分析+改进决策，分工明确；CLEAN标记跳过全流程防无意义；冷却期+提案上限防过度进化
- **通用结论**：用钩子采数据+LLM做决策分工协作

## ✅ 11. 项目级配置从零搭建
- **做法**：创建.claude/settings.json，一次性配齐mcpServers+hooks+permissions三大模块，权限放行常用命令拦截危险操作
- **可参考的点**：settings.json放项目根目录.claude/下自动加载；permissions allow放行高频命令减少打断；hook脚本用File路径引用避免长命令嵌在JSON里
- **通用结论**：项目配置三大件MCP、钩子、权限一把配

---

## 12. ✅ Windows MCP配置两件套：.mcp.json + cmd /c
- **场景**：4个MCP服务器全连不上，配置写在settings.json里，npx直接调用。搜索后发现Windows必须用.mcp.json + cmd /c包装
- **可参考的点**：Windows子进程无shell上下文，npx.cmd无法解析，必须显式调用cmd /c；配置文件格式和位置因平台而异，先查平台特定文档再动手
- **通用结论**：Windows MCP必须.cp.json+cmd/c

---

## 核心铁律
1. **先想后动** — 说不能前扫工具，修bug前写验证，加代码前问简化
2. **一步到位** — 输出最终文件，不等催不等确认
3. **环境一致** — 验证和部署用同一个环境
