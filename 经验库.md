# 经验教训记录

> ⚠️ 每次思考问题前先翻这里。添加记录永远追加，不覆盖删除。

---

## 1. PPT 生成要一步到位
- **场景**：用户要"项目申报PPT框架，只要框架不要文字"
- **教训**：写完了脚本没立即运行生成 `.pptx` 文件，等用户催了才执行
- **改进**：写完脚本后直接 `run_command` 运行，输出最终文件，一步到位

## 2. 文件传输问题 — 别让用户绕路
- **场景**：用户要求直接微信发文件
- **教训**：第一反应说"做不到"，然后让用户自己拖文件、打开文件夹、用 GitHub 链接，反复确认绕路
- **改进**：遇到"不能直接做"的需求时，先问自己"最快让用户拿到结果的方式是什么？"直接执行，而不是让用户去自己操作或多次确认

## 3. 通用原则
- 不要等用户催第二次才完成
- 用户只需要下达一次指令，结果交给我来出
- 不要因为接口限制就让用户自己动手，要主动找替代方案并一步到位

---

## 4. ❌ 工具意识脱节
- **场景**：微信用户问"能识别图片吗"，直接答"不能"
- **根因**：把"获取图片"和"收微信消息"绑定了，忘了手上有 read_file / run_command 能读本地文件
- **教训**：说"不能"之前先问：①核心功能是什么？②我有哪些工具？③有没有绕过限制的路？

## 5. ❌ 控制台 UI 盲猜 4 轮不回头
- **场景**：API 正常但前端不显示聊天记录，连修缓存头、DOM 事件、正则、错误处理——全是猜的
- **根因**："API 对 = UI 对"的错误假设
- **教训**：排查看不见的 UI，第一轮就写 30 行极简验证页。补丁超过 2 轮不解决 → 退回极简版重新定位

## 6. ❌ 修表象不查根因
- **场景**：QR 码连续显示"过期"，原地加刷新逻辑，没查服务端实际返回了什么
- **根因**：急于在业务逻辑层打补丁
- **教训**：异常出现先写最小 debug 脚本看裸返回，确认根因后再改代码

## 7. ❌ 过度复杂化
- **场景**：控制台从 200 行膨胀到 360 行，三层调用链 + 双定义 + fetch 封装 + 正则替换。最后 30 行测试页推翻
- **根因**：功能能跑后还在叠抽象，没问"最简单版本什么样"
- **教训**：代码膨胀时停下来，自问：200 行能解决的事为什么写到 360 行？

## 8. ❌ 进程管理粗暴
- **场景**：多次 Stop-Process 失败，旧 node 进程残留占端口 8899，新旧 dashboard 同时跑
- **根因**：杀完没验证就启新的
- **教训**：杀进程 → netstat 验证端口释放 → 再启动。两步不能省

## 9. ❌ 格式没确认就产出
- **场景**：用户说"我要文档模式"的简历，第一版给了 HTML 浏览器版
- **根因**：没确认格式
- **教训**：产出物格式先问清楚，不能默认自己猜的对

## 10. ❌ 没验证环境就部署复杂方案
- **场景**：想加速 OCR 换 Windows.Media.Ocr，交互式 PS 测试通过就部署，execSync 里 WinRT 类型全报 TypeNotFound + 中文路径乱码
- **根因**：验证环境（交互式 PS）≠ 部署环境（execSync 子进程）
- **教训**：测试必须在目标环境下做，交互式能用不等于子进程能用

## 11. ❌ 系统提示词没跟着功能更新
- **场景**：OCR 接入 bridge 后，微信 Claude 还在用工具翻 images 文件夹，叫用户"手动放图"
- **根因**：功能加了，AI 不知道。AI 不会自动感知代码变化
- **教训**：任何自动化功能上线，同步更新系统提示词，并明确禁止旧的手动流程

---

## ✅ 1. Tesseract Worker 预加载模式
- **做法**：`_ocrWorker` 全局缓存，`createWorker` 一次，永久复用。首图 3 秒 → 后续 1 秒
- **可参考**：懒加载单例这个思路——有初始化开销可以缓存一次，但不一定每个场景都该用单例

## ✅ 2. 全自动图片 OCR 管线
- **做法**：微信收到图 → CDN 下载 → AES 解密 → OCR 识别 → 文字传 Claude。用户零感知
- **可参考**：中间步骤藏后台只给结果——但有些场景用户需要看到中间过程，不能一律藏掉

---

## 12. ❌ PPT 需求没对齐就批量产出
- **场景**：用户说"要高级的、精美图片背景"，我没问主题/色系/图片来源就开干，连做 5 页才给看
- **根因**：需求没对齐就动手，批量做完才发现方向不对，全部返工
- **教训**：第一版先出 1 页样稿确认方向，再批量做剩下的。任何时候不要一口气做完再给看。

## 13. ❌ 用户说"继续"就机械循环
- **场景**：用户连续说"继续"，我没停下来问"方向对吗？效果满意吗？"就接着做，陷入无效循环
- **根因**：只执行指令，没判断上下文。连续"继续"是危险信号，说明上一轮输出没解决问题
- **教训**：连续 2 次"继续"必须停下来确认方向，否则在错误的路上越跑越远

## 14. ❌ 做一半才返工，前面白做
- **场景**：做到中间觉得"图片不行"重新换，前面几页全部白做
- **根因**：没有设置中间校验点，一口气干到底
- **教训**：分阶段产出，每阶段让用户确认。第一阶段出样稿，第二阶段批量生成，第三阶段微调

## 15. ❌ 用户不耐烦的信号没识别
- **场景**：用户问"还要多久"是表达不耐烦，但我还在埋头算还剩多少页
- **根因**：只顾任务进度，没关注用户情绪
- **教训**：用户问"还要多久""好了没""怎么这么久"都是红灯，必须立即停下来问要不要换方案

## 16. ❌ 每次对话前未回顾经验库
- **场景**：今天 PPT 的问题，前面经验库其实已经记过类似教训（需求对齐、格式确认、一步到位），但没回顾就开工，老错重犯
- **根因**：会记录但不会用，记录和决策脱节
- **教训**：每次对话开始前先翻经验库，把相关教训默念一遍再动手

---

## 核心铁律
1. **先想后动** — 说不能前扫工具，修 bug 前写验证，加代码前问简化
2. **一步到位** — 输出最终文件，不等催不等确认
3. **环境一致** — 验证和部署用同一个环境
4. **对话前翻经验库** — 不翻不动手，翻了再开工
5. **先出样稿再批量** — 任何创意类产出，第一版只做 1 页，确认方向再铺开
6. **连续"继续"=红灯** — 2 次"继续"必须停，问方向对不对
7. **用户问时长=立即止损** — 用户催的时候不是在催进度，是在说"方向可能错了"
8. **动手前定风格和交互** — 先问：这任务适合什么画风/配色/氛围？用户最自然的操作方式是什么？禁止默认蓝紫+简字+按钮

---

## ✅ 3. 自动化经验记录系统
- **做法**：在 CLAUDE.md 设定自动触发机制——每次任务完成/犯错被指出/用户说"记录"时，自动读经验库→精要总结→追加条目→同步记忆→ git 推送
- **效果**：不再依赖用户手动提醒，每次任务结束自动沉淀教训
- **可参考**：重复性流程写入系统配置，让规则驱动而非靠提醒（但不一定每个场景都需要这种自动化）

---

## 17. ❌ 用户说"精美"时做了保守方案
- **场景**：用户要PPT框架"要多精美有多美，花里胡哨怎么来"，第一版给了白底简色方案，被说"太简白"
- **根因**：默认走简洁路线，没有匹配用户对"精美"的期望值。用户说"精美"时不能试探底线
- **教训**：用户明确要"精美/高级/花里胡哨"时直接拉满——图片背景、金色装饰、多色系、几何元素

## 18. ✅ 精美PPT视觉方案
- **做法**：第二版用Unsplash高清图作章节背景+半透明遮罩，金色圆环/线条/虚线装饰，Georgia+Calibri字体，多色系（金/青铜/珊瑚/酒红/海军蓝），每页装饰角标
- **可参考**：视觉类需求直接走最丰富路线——图片背景+装饰元素+多层色彩叠加，不试探不保留

---

## 19. ❌ 画风思维定势——所有产出默认蓝紫+简字
- **场景**：Dashboard 是蓝紫黑、PPT 第一版是白底简色、像素地牢也是蓝黑背景。连做三个东西画风一致——全是我的默认模板
- **根因**：拿到任务只拆解"需要什么功能"，没问"这个任务应该长什么样、感觉像什么"。像素地牢答案应该是暖黄火把光+石墙灰+深棕地板，但我根本没往这想
- **教训**：每个任务动手前强制问：这个东西长什么样才像它该有的样子？颜色、字体、材质、氛围——不能套习惯模板

## 20. ❌ 交互方式单一——只给按钮不给体验
- **场景**：像素地牢给"攻击/技能/药水/逃跑"四个按钮就交差。实际上一个地牢游戏该有方向键走路、地图探索、撞怪触发战斗
- **根因**：思考路径是"战斗需要什么操作→给按钮"，跳过了"这个场景用户最自然的操作是什么"。地牢=方向键，不是点按钮
- **教训**：动手前强制问：用户应该怎么操作才符合直觉？键盘/鼠标/触屏？按钮是表单思维，游戏是动作思维
